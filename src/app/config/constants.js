import { app } from '../module';

app.constant('initialLocale', 'en');
app.constant('supportedLanguages', [
            {id:'en', name : 'English'},
            {id:'es', name : 'Español'}
        ]);